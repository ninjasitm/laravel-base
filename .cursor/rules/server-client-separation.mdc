---
description: Rules for separating server and client code
globs:
alwaysApply: agent
---

- Never import server-only code in client components.
- Use API endpoints to expose server functionality to clients.
- Create shared types for cross-boundary use.
- Use appropriate data fetching patterns for the framework.
- Keep host-app frontend/client concerns separate from package server code and APIs.
